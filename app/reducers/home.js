'use strict';

const initialState = {
    showFilter: false,
    filterMounted: false,
};

const home = function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export default home;
