'use strict';

const initialState = {
    showCate: false
};

const home = function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export default home;
